import React from "react"
import data from "./data"

const DieEmmora = () => {
  return (
    <div>
      <img />
      <p>{data.title}</p>
      <p>{data.subTitle}</p>
      <button>{data.buttonLabel}</button>
    </div>
  )
}

export default DieEmmora
